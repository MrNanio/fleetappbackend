<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd">
    <changeSet id="1" author="nankiewic">
        <sql splitStatements="false">

            CREATE TABLE fuel_types (
            id BIGINT NOT NULL,
            name VARCHAR(7)
<!--            PRIMARY KEY(id)-->
            );

<!--            CREATE TABLE insurance_types (-->
<!--            id BIGINT NOT NULL,-->
<!--            name VARCHAR(45) NOT NULL-->
<!--            );-->

<!--            CREATE TABLE roles (-->
<!--            id BIGINT NOT NULL,-->
<!--            name VARCHAR(45) NOT NULL,-->
<!--            PRIMARY KEY(id)-->
<!--            );-->

<!--            CREATE TABLE vehicle_status (-->
<!--            id BIGINT PRIMARY KEY,-->
<!--            name VARCHAR(15) NOT NULL-->
<!--            );-->

<!--            CREATE TABLE address (-->
<!--            id BIGINT PRIMARY KEY,-->
<!--            city VARCHAR(45) NOT NULL,-->
<!--            street VARCHAR(45),-->
<!--            building_number VARCHAR(5) NOT NULL,-->
<!--            flat_number VARCHAR(7),-->
<!--            postal_code VARCHAR(7) NOT NULL,-->
<!--            &lt;!&ndash;            id_user_data BIGINT NOT NULL REFERENCES "user_data"(id)&ndash;&gt;-->
<!--            )-->

<!--            CREATE TABLE users (-->
<!--            id BIGINT PRIMARY KEY,-->
<!--            email VARCHAR(45) NOT NULL,-->
<!--            password VARCHAR(45) NOT NULL,-->
<!--            reset_code VARCHAR(45),-->
<!--            reset_at DATETIME,-->
<!--            created_at DATETIME,-->
<!--            last_login_at DATETIME,-->
<!--            is_enabled BOOLEAN DEFAULT 'false',-->
<!--            id_user_account_status,-->
<!--            id_manager BIGINT NOT NULL REFERENCES users(id),-->
<!--            &lt;!&ndash;            id_user_data BIGINT NOT NULL REFERENCES "user_data"(id),&ndash;&gt;-->
<!--&lt;!&ndash;            id_verification_token BIGINT NOT NULL REFERENCES verification_token(id),&ndash;&gt;-->
<!--            id_role BIGINT NOT NULL REFERENCES roles(id)-->
<!--            )-->

<!--            CREATE TABLE verification_token (-->
<!--            id BIGINT PRIMARY KEY,-->
<!--            token VARCHAR,-->
<!--            expiry_date DATETIME,-->
<!--            id_user BIGINT NOT NULL REFERENCES users(id)-->
<!--            )-->

<!--            CREATE TABLE user_account_status (-->
<!--            id BIGINT PRIMARY KEY,-->
<!--            name VARCHAR(45) NOT NULL,-->
<!--            )-->

<!--            CREATE TABLE user_data (-->
<!--            id BIGINT PRIMARY KEY,-->
<!--            name VARCHAR(45) NOT NULL,-->
<!--            surname VARCHAR(45) NOT NULL,-->
<!--            phone_number VARCHAR(15) NOT NULL,-->
<!--            &lt;!&ndash;            id_user BIGINT NOT NULL REFERENCES "users"(id)&ndash;&gt;-->
<!--            &lt;!&ndash;            id_address BIGINT NOT NULL REFERENCES "address"(id)&ndash;&gt;-->
<!--            )-->

<!--            CREATE TABLE vehicles (-->
<!--            id BIGINT PRIMARY KEY,-->
<!--            id_vehicle_make BIGINT NOT NULL REFERENCES vehicles_makes(id),-->
<!--            model VARCHAR(45) NOT NULL,-->
<!--            year VARCHAR(4) NOT NULL,-->
<!--            color VARCHAR(45) NOT NULL,-->
<!--            mileage VARCHAR(7) NOT NULL,-->
<!--            vin_number VARCHAR(17) NOT NULL,-->
<!--            vehicle_registration_number VARCHAR(10) NOT NULL,-->
<!--            id_fuel_type BIGINT NOT NULL REFERENCES fuel_type(id),-->
<!--            city_fuel_consumption DECIMAL(4, 2) NOT NULL,-->
<!--            country_fuel_consumption DECIMAL(4, 2) NOT NULL,-->
<!--            average_fuel_consumption DECIMAL(4, 2) NOT NULL,-->
<!--            id_vehicle_status BIGINT NOT NULL REFERENCES vehicle_status(id),-->
<!--            id_user BIGINT NOT NULL REFERENCES users(id)-->
<!--            )-->

<!--            CREATE TABLE current_vehicle_users (-->
<!--            id BIGINT PRIMARY KEY,-->
<!--            id_user BIGINT NOT NULL REFERENCES users(id)-->
<!--            id_vehicle BIGINT NOT NULL REFERENCES vehicles(id)-->
<!--            )-->

<!--            CREATE TABLE vehicle_inspections (-->
<!--            id BIGINT PRIMARY KEY,-->
<!--            inspection_date DATE NOT NULL,-->
<!--            expiration_date DATE NOT NULL,-->
<!--            cost DECIMAL(5, 2) NOT NULL,-->
<!--            description VARCHAR(45),-->
<!--            id_vehicle BIGINT NOT NULL REFERENCES vehicles(id)-->
<!--            )-->

<!--            CREATE TABLE vehicle_insurances (-->
<!--            id BIGINT PRIMARY KEY,-->
<!--            effective_date DATE NOT NULL,-->
<!--            expiration_date DATE NOT NULL,-->
<!--            cost DECIMAL(8, 2) NOT NULL,-->
<!--            policy_number VARCHAR(25) NOT NULL,-->
<!--            description VARCHAR(100),-->
<!--            id_vehicle BIGINT NOT NULL REFERENCES vehicles(id),-->
<!--            id_insurance_type BIGINT NOT NULL REFERENCES insurance_types(id)-->
<!--            )-->

<!--            CREATE TABLE vehicle_makes (-->
<!--            id BIGINT PRIMARY KEY,-->
<!--            name VARCHAR-->
<!--            )-->

<!--            CREATE TABLE vehicle_refueling (-->
<!--            id BIGINT PRIMARY KEY,-->
<!--            refueling_date DATE NOT NULL,-->
<!--            litre VARCHAR,-->
<!--            cost DECIMAL(8, 2),-->
<!--            description VARCHAR(244),-->
<!--            id_vehicle BIGINT NOT NULL REFERENCES vehicles(id),-->
<!--            id_user BIGINT NOT NULL REFERENCES users(id),-->
<!--            )-->

<!--            CREATE TABLE vehicle_repairs (-->
<!--            id BIGINT PRIMARY KEY,-->
<!--            repair_date DATE NOT NULL,-->
<!--            description VARCHAR(555) NOT NULL,-->
<!--            cost DECIMAL(8, 2),-->
<!--            title VARCHAR(20) NOT NULL,-->
<!--            id_vehicle BIGINT NOT NULL REFERENCES vehicles(id)-->
<!--            )-->

<!--            CREATE TABLE vehicle_use (-->
<!--            id BIGINT PRIMARY KEY,-->
<!--            trip SMALLINT NOT NULL,-->
<!--            trip_date DATE NOT NULL,-->
<!--            description VARCHAR(244),-->
<!--            trip_type VARCHAR(7),-->
<!--            id_vehicle BIGINT NOT NULL REFERENCES vehicles(id),-->
<!--            id_user BIGINT NOT NULL REFERENCES users(id)-->
<!--            )-->
        </sql>

        <!--        <createTable tableName="person">-->
        <!--            <column name="address" type="varchar(255)"/>-->
        <!--        </createTable>-->
    </changeSet>
</databaseChangeLog>